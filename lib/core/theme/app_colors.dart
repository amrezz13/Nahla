import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFFF5A623);       // Honey gold
  static const Color primaryDark = Color(0xFFD4891C);   // Dark honey
  static const Color primaryLight = Color(0xFFFFD080);  // Light honey

  // Secondary colors
  static const Color secondary = Color(0xFF5D4E37);     // Brown (wood/hive)
  static const Color secondaryLight = Color(0xFF8B7355);

  // Background colors
  static const Color background = Color(0xFFFFFBF5);    // Warm white
  static const Color surface = Color(0xFFFFFFFF);       // Pure white
  static const Color cardBg = Color(0xFFFFF8EC);        // Cream

  // Text colors
  static const Color textPrimary = Color(0xFF2D2D2D);   // Dark gray
  static const Color textSecondary = Color(0xFF757575); // Medium gray
  static const Color textLight = Color(0xFFFFFFFF);     // White

  // Status colors
  static const Color success = Color(0xFF4CAF50);       // Green
  static const Color warning = Color(0xFFFF9800);       // Orange
  static const Color error = Color(0xFFE53935);         // Red
  static const Color info = Color(0xFF2196F3);          // Blue
}
